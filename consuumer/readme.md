
```
TL;DR

Multi-language apps (Java + Python): PEM is the way to go

Java 21 supports PEM via in-memory SSLContext, so no JKS/PKCS12 needed

Python expects PEM anyway

PKCS12 is Java-friendly, Python-unfriendly
```
